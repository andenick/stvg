// Placeholder to ensure the static library has at least one translation unit.
// The engine is currently header-only; implementation files will be added
// as classes grow beyond what's reasonable in headers.
namespace stvg { void _engine_placeholder() {} }
